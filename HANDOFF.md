# HANDOFF — состояние на момент паузы и отправная точка

Сессия была посвящена: (а) тесту итогового 1v1-солвера против planner'а, (б) построению
DeepStack/ReBeL-инфраструктуры (value network + depth-limited ресоляинг) для 3v3.
Полная картина игры и архитектуры — `SOLVER_STATE.md`. Здесь только то, что нужно для
возобновления без перечитывания всей сессии.

## Что готово и работает

### 1v1 (сильный, решён)
- Полный full-history CFR солвер (`src/cote_megaverse/solver_tree_fh.py`) сходится:
  cap=6, total expl **0.0496** при 1400 итераций. Чекпоинт локально:
  `server_out_fh/ckpt_fh_cap6.pkl` (gitignored, `.pkl`), парамсы `params_fh_cap6.pkl`.
- Таблица значений `cote_cfr/1v1_table_cap6.csv` (3476 состояний, матчап-независима по хитам).
- Тест vs planner (`test_1v1_solver_vs_planner.py`, 100 игр, C/C 10000/2000):
  - как B (второй ход): **0 поражений за 50 игр** (2W/48D) — непроигрываем;
  - как A (первый ход): 25W/17L/8D — уязвим, причина **explA=0.049 vs explB=0.0005**
    (первый ходящий сходится последним, структурная асимметрия из `traj_fh_cap6.csv`).

### Rust-движок (`cote_cfr`, PyO3, abi3-py39)
- `MicroTree` — класс с **подключаемыми листовыми значениями** (хук DeepStack/ReBeL):
  `leaf_states()` отдаёт flat-состояния 2v2/3v3 листьев (1v1 исключены — их решает точная
  таблица), `solve(iters, gamma, override)` гоняет CFR с learned-листьями,
  `strategy()` → (actions, probs, value).
- `solve_micro` / `solve_micro_belief` — обычный ресоляинг (belief-корень).
- Perfect-recall инфосеты через `(obs, prev_iid)` — это дало **×100 скорости**
  (сборка depth-4 3v3: 140с → 1.2с). Инфосеты идентичны full-history (валидировано).
- Прунинг: доминанс по `sh_opp` **неверен** в imperfect info; support-прунинг искажает
  без зрелого burn-in. Оба выключены, `prune=true` — экспериментальный.

### Бот vs planner (`cfr_vs_planner.py`, `src/cote_megaverse/cfr_bot.py`)
- Бот играет легально, дерётся (вес тел 0.35 убрал «черепаху»).
- **Но стабильно проигрывает planner'у при depth 3**: material 0/5, vnet1 1/5, vnet2 0/5,
  vnet3 (эндшпиль) 1/8. Вывод: качества не хватает, см. «Почему не бьёт».

### DeepStack/ReBeL инфраструктура (работает end-to-end)
- `server/gen_value_data.py` — пайплайн лейблов: сэмпл позиции (50% эндшпиль с низким HP,
  50% мидгейм) → belief-корень → MicroTree → корневая ценность. Логгер: мгновенная
  скорость + доступная RAM. Воркеры: **4-6** (8 перегружают 16GB), при загрузке машины
  время ~1-5с/поз (разброс: belief-корень R=0..4 → дерево ×1..×5).
- `server/train_value_net.py` — сеть (MLP 56→128→128→1, actor-relative + belief),
  лейбл = ценность ходящего. Сохраняет `.pt` + `.npz`.
- `server/value_leaf.py` — оценка листьев сетью. **Критично: torch-free на top-level**
  (иначе Windows-spawn воркеры дедлокают на импорте torch); воркеры — чистый numpy
  (`NumpyValueNet`). `REPO` обязан быть в `sys.path` для импортов в воркерах.
- Модели в git: `table_out/vnet1.v2.v3` (`.npz` + `.pt`).

## Почему бот пока не бьёт planner'а (честно)
1. **MAE — иллюзия**: сеть учится предсказывать мнение «слепого» дерева (material-листья
   на глубине 3), а не истинную ценность. Garbage In → Garbage Out.
2. **Глубина 3 не видит смертей** в full-HP 3v3 (килл за 5-8 ходов) → нет Ground Truth
   (+1/-1) в горизонте. Эндшпильный сэмплер (50% низкий HP) добавлен, но данных мало.
3. **Данных катастрофически мало**: 200-3000 позиций против сотен тысяч у AlphaZero.
4. **Planner сильный** (тюненные эвристики + поиск), это не слабый противник.

## Производительность машины (i5-13500H, 16GB, CPU-only torch)
- Нет монотонной утечки/GC-деградации (проверено чистым прогоном 200 поз: inst время
  колеблется 1-5с/поз, RAM стабильна).
- Рост времени в прогоне на 3000 = кумулятивный логгер + **сироты-воркеры от прерванных
  запусков** (находил 6 брошенных python, 3.9GB) + 8 воркеров на 16GB + параллельная
  работа пользователя. Перед прогонами: `Get-Process python | Stop-Process -Force`.
- Colab T4 доступен, но CPU там слабее, сессии 5ч — для генерации данных невыгодно.

## Отправная точка новой сессии

**Решение, которое висело в конце:** продолжать ReBeL-кампанию (нужен план на объёмы)
или пересмотреть цель. Если продолжать — конкретный план:

1. **Увеличить данные на порядки**: 20-50k позиций (4-6 воркеров, ~2-4 ч на 10k при
   текущей скорости; ночь). Генерация: `python server/gen_value_data.py --n 20000
   --workers 4 --net table_out/vnet3.npz --out table_out/vdata_20k.pkl` (перед этим
   зачистить python-процессы).
2. **Несколько ReBeL-итераций**: генерация с сетью на листьях → ретрейн → повтор
   (механика готова, `--net` + `train_value_net.py`).
3. **Увеличить глубину до 4-5** (дороже, но ближе к киллам; на 3v3 depth-4 ~9-26с/ход).
4. **Belief-пропагация на листьях** вместо uniform (спред до 1.89 — известное
   приближение).
5. Тест: `python cfr_vs_planner.py --games 30 --net table_out/vnetN.npz`.

**Ключевые команды/файлы для возобновления:** `cote_cfr` (Rust), `server/gen_value_data.py`,
`server/train_value_net.py`, `server/value_leaf.py`, `src/cote_megaverse/cfr_bot.py`,
`cfr_vs_planner.py`, `test_1v1_solver_vs_planner.py`, чекпоинт `server_out_fh/`.

**Ловушки, не повторять:** не гонять 8 воркеров на длинных прогонах; не запускать тяжёлое
без смоук-теста и озвученного ожидаемого времени; torch-импорт только вне воркеров;
`REPO` в sys.path; чистить осиротевшие воркеры; не строить полные деревья (OOM при cap≥7).
