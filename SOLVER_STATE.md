# COTE Megaverse — состояние проекта и технические аспекты

Этот документ — самодостаточное описание игры, текущего состояния солвера, ключевых
открытий и архитектурных решений. Написан так, чтобы внешний помощник/консультант
мог быстро войти в контекст и **не повторял уже найденные и доказанные ошибки**.

Официальная спецификация игры: `AGENT.md`. Код: `src/cote_megaverse/`.

---

## 1. Игра (правила, важные для солвера)

**Состав.** 3 на 3 персонажа. У каждого: тип A/B/C/D, HP 5700-6300, ATK 1900-2100.
Дубли типов разрешены.

**Типы.** Цикл преимущества `A > B > C > D > A`. Атака по следующему в цикле = ×1.3,
по предыдущему = ×0.7, иначе ×1.0.

**Ходы.** На своём ходу сторона получает бюджет:
`budget = base(turn) + bank`, где `base`: ход 1 → 1, 2-4 → 2, 5-6 → 3, 7+ → 4;
`bank` 0..4 (свой скрытый запас). Итог ≤ 8. **Все действия обязаны быть потрачены.**

Аллокация: `атаки + щиты + бонус + (1 если свитч) = budget`. Максимум 1 свитч за ход.

**Щиты.** Живут один ход противника: созданные на ходу N защищают от атаки на ходу
N+1, после чего сгорают (независимо от того, заблокировали ли что-то). Каждый щит
блокирует 1 атаку.

**Банк.** Скрыт, пока хранится. На своём следующем ходу тратится в бюджет
(раскрывается через публичный бюджет). Кап 4.

**Свитч.** Добровольная смена активного на любого живого неактивного. Стоит 1
действие. Доступен **в любой свой ход**, не только после нокаута. При смерти
активного — бесплатная промоция: первый живой из собственного стека.

**Урон.** Агрегируется за обмен и округляется один раз до 100:
`round(atk * mult * landed / 100) * 100`.

**Скрытая информация (единственная).** Сторона не видит сплит противника
`(щиты, банк)`, а только **сумму R = щиты + банк**. R публична (бюджет противника
минус его атаки). Это ядро игры: блеф «щит vs банк».

**Исходы.** Утилита с точки зрения A: +1 победа A, −1 победа B, 0 ничья.
Ничья — по достижении turn cap. Применяется γ-дисконт (0.995) против затягивания.

**Преимущество первого хода.** Структурное, измерено: первый ходящий имеет
ценность ~+0.097 (≈71% решённых партий, ~77% ничьих на cap=6). Это свойство игры,
не солвера.

## 2. Абстракция «хиты» и матчап-независимость

**Абстракция.** HP каждого активного → `hits = ceil(HP / (atk_противника · mult))` —
число попавших ударов до смерти. Попавший удар = −1 к хитам. Урон в абстракции без
округления до 100 (единственная ошибка — округление реальной игры, <100 HP/обмен).

**Матчап-независимость (доказано эмпирически).** Значение 1v1-игры зависит только от
пары `(hitsA, hitsB)`, а не от конкретных типов/атак/HP. Разные команды с одинаковыми
хитами дают одинаковое значение, одинаковый граф, одинаковые инфосеты (проверено на
сетке типов/атак/HP). Это значит: 1v1-таблица индексируется парами хитов.

**Почему нельзя хранить «удары» в мульти-чар игре (важно!).** В 1v1 «удары» работают,
потому что матчап фиксирован. При свитче/промоции противник меняется, и «удары до
смерти» относительны к его урону. Конвертация ударов между матчапами теряет точность:
`ceil` стирает точный HP, ошибка до ±2 ударов. Пример: HP=4000, против B1 (урон 2000)
это 2 удара; против B2 (урон 1330) — реально 4 удара, но из «2 удара» получается 2-3.
**Поэтому в мульти-чар состоянии хранится точный HP в единицах 10** (урон и HP кратны
10 → точно), а на слой решений («атаки до смерти») конвертация делается на лету:
`hits = ceil(HP_units / dmg_units)`. Решения/ценности всегда на слое ударов, HP — это
точная «валюта» для пересчёта при смене матчапа.

## 3. 1v1-солвер и ключевое открытие: imperfect recall

**Проблема.** Исходный солвер (DAG, `solver1v1.py`, info_key схлопывает сплит в сумму)
**не сходился**: expl осциллировал, плато 0.3-0.6 на cap≥9, «эталон» 0.121 после
173k итераций.

**Причина (доказана).** `info_key = (turn, hA, hB, свой банк, R)` даёт **imperfect
recall**: игрок «забывает» свою историю наблюдений (прошлые раскрытия щитов/банков),
которая влияет на веру в текущий скрытый сплит. Разные истории к одному состоянию
склеиваются в один инфосет. CFR сходится только при perfect recall.

**Доказательства:**
- На чистом дереве (без DAG-транспозиций) collapsed-инфосеты тоже не сходятся →
транспозиции DAG **не причина** (дерево ≡ DAG по результатам).
- Full-history инфосеты (полная последовательность наблюдений игрока) **сходятся**.

**Фикс.** `solver_tree_fh.py` — полное дерево (каждая история = свой узел), инфосеты
= полная последовательность наблюдений игрока (perfect recall). Сходится.

**Замеры сходимости (full-history):**
- cap=5 hp=6000: 0.20@50 → 0.10@150 → 0.005@350 → 0.0028@600.
- cap=6 hp=10000: 0.25@50 → 0.09@100 → 0.055@600 → **0.0496@1400** (пересекли 0.05).
- Асимметрия `explA >> explB` — структурна (первый ходящий сходится последним), не баг.

**Вера (belief).** Скрытое — сплит `(щиты, банк)` с суммой R → belief = распределение
по R+1 вариантам (до ~9). Механизм передачи в солвер — `root_weights` (веса стартовых
состояний, где перечисляются сплиты). Он уже реализован.

**Belief-зависимость значений (важно!).** Значение 1v1 в абстрактном состоянии
**зависит от пути/веры**, не только от состояния: 58.5% состояний имеют разброс
значений >0.1 (медиана 0.28, до 1.89). Поэтому **статическая таблица «состояние →
значение» неверна**; для верхних уровней нужен ре-солвинг от belief (субгейм-солвинг).

## 4. Масштаб (замерено, HP в единицах-10)

| Уровень | cap | состояний | узлов дерева | реально? |
|---|---|---|---|---|
| 1v1 | 6 | — | 5.1M (28k full-history инфосетов) | да (0.38-0.42 it/s, 1 ядро) |
| 2v2 | 6 | 645k | ~1.4M | да (ноут) |
| 2v2 | 8 | 5.1M | ~11M | сервер, тяжело |
| 3v3 | 4 | 97k | ~210k | да |
| 3v3 | 6 | 15M | ~33M | 40-60GB RAM, ~6-8ч на 1 ядре |
| 3v3 до 1-го убийства | 6 | 13.3M | ~30M | в основном ничьи (кап мал) |

- Рост состояния ~×12-14 за уровень капа.
- **Итерация memory-bound**: потоки не ускоряют один прогон. 30-64 ядра дают ценность
  только через параллельные независимые задачи или распараллеливание внутренностей
  солвера (инженерия, не сделано).

## 5. Что построено (код)

- `src/cote_megaverse/solver1v1.py` — DAG-CFR (collapsed). Эталон/лаборатория. Не сходится
  (imperfect recall).
- `src/cote_megaverse/solver_tree_fh.py` — **рабочий**: полное дерево + perfect-recall
  инфосеты + `first_move` (0/1) + `start_states` (mid-game старт). Сходится.
- `src/cote_megaverse/trunk.py` — правила полной 3v3: HP каждого персонажа (единицы-10),
  порядок стека, свитч, промоция, банки/щиты, скрытый сплит. Протестировано (8 сценариев).
- `server/run_full_history.py` — чекпоинт-раннер (резюм, траектория expl, дамп таблицы значений).
- `server/run_table.py` — параллельный грид матчапов + дамп per-state значений.
- `docs/trunk_solving_plan.md` — план субгейм-солвинга (не в git, `docs/` в .gitignore).
- Чекпоинт равновесия 1v1: cap=6 hp=10000, total expl 0.0496, W/L/D = 16.6/6.6/76.8.
- `cote_cfr/` — Rust-крейт (PyO3, abi3-py39): порт правил транка + движок `solve_micro`
  (depth-limited FH-CFR микро-дерево, полный цикл в Rust). `solve_micro_belief` —
  корень из нескольких сплитов с весами (вера): инфосет ходящего общий, противник
  различает свои сплиты. Листовая оценка 1v1 — точная таблица `1v1_table_cap6.csv`
  (3476 состояний, каноническая вера); 2v2/3v3 листья — material-эвристика.
  Замеры: 1v1 depth=2 ~1мс, 3v3 depth=3/500 итер ~13.4с, depth=4 — таймаут.

## 6. План: субгейм-солвинг / CFR-D (как DeepStack в покере)

**Идея.** Не решать всю 3v3 целиком (комбинаторно нереально). Искать по дереву
ограниченной «глубины» + на листьях подставлять **точное значение 1v1 от belief**.
В отличие от DeepStack (пришлось учить сеть для оценки листьев) у нас оценка листьев —
**точное равновесие 1v1**.

**Иерархия (рекурсивная, по числу живых):**
```
3v3: дерево CFR до первого убийства (граница 2v2), листья = значение 2v2 от belief
2v2: дерево CFR до 1v1-границы, листья = значение 1v1 от belief
1v1: точное равновесие (есть)
```
Каждый уровень доходит только до следующего убийства — главное сокращение глубины.
Смерть → промоция → значение нижнего уровня (это решает «дуэль ≠ конец игры»).

**Вера — механизм связи.** На границе belief передаётся в нижний солвер через
`root_weights`. Требуется ре-солвинг (значение подыгры зависит от входящего belief).

**Глубина мала.** Внутри дуэли 1v1-солвер прокидывает все ходы банк/щит/килл.
Дерево решает уровень команд: продолжить дуэль или свитч, и как ответит противник
(2-4 «решения уровня свитча», не 20 ходов).

**Тонкости, которые уже понятны:**
- «Остаться в дуэли» — **рекурсивный узел**, а не терминал: иначе теряется опция
  будущего свитча (занижается ценность «остаться»).
- Свитч — обычное действие дерева, его стоимость (1 действие) учитывает дерево.
- Интерфейс выхода 1v1 должен давать не только значение, но и **распределение исходов**
  (кто умрёт, кто выживет с каким HP и банком) — чтобы дерево знало, куда идёт игра
  после килла (промоция).

## 7. Подводные камни (чтобы помощник не повторял ошибки)

1. **Не использовать collapsed info_key** (imperfect recall → нет сходимости, доказано).
2. **Не строить статическую таблицу «состояние → значение»** (belief-зависимость,
   spread до 1.89). Нужен ре-солвинг от веры.
3. **Не абстрагировать HP в грубые бакеты** (свежий/раненый/критический): игра резкая,
   «3 vs 4 атаки» = килл или нет. Резкость сохраняет только точное дерево (HP в
   единицах-10). HP/100 (ошибка <100 HP, sub-hit) — крайний компромисс, грубые бакеты — нет.
4. **Не хранить «удары до смерти» вместо HP в мульти-чар** (конвертация при смене
   матчапа теряет ±2 удара). Хранить точный HP, считать удары на лету.
5. **Потоки не ускоряют один прогон** (memory-bound, np.bincount/np.add.at однопоточные).
6. **Матчап-независимость**: не решать каждую комбинацию типов/атак/HP — таблица по
   парам хитов покрывает всё.
7. **Bootstrap-схема «свитч = значение матчапа из таблицы» сама по себе неверна**: не
   учитывает HP скамейки, банки/щиты/ход, и главное — промоцию после смерти (1v1-значение
   = терминальная дуэль, а не фаза). Только рекурсия по пост-смертному продолжению (см. §6).

## 7a. Прунинг действий в микро-дереве — что доказано (Rust)

- **Доминанс-прунинг по `sh_opp` НЕВЕРЕН в imperfect information.** «Атаки, полностью
  заблокированные щитами, → перевести в щиты/банк» звучит безопасно, но: инфосет видит
  только R = sh+банк, а не sh по отдельности. Узлы одного инфосета (разные сплиты того же R)
  получали бы разные списки действий → CFR ломается (index-out-of-bounds, неверные значения).
  Кроме того, действие, доминируемое при одном сплите, может быть критично против другого
  (смешанная стратегия по вере). Проверено на 1v1-регрессии: значения 0.9950/0.0044/0.0481
  ломались до 0.49.
- **Support-прунинг (по avg-стратегии, с пересборкой) искажает, если burn-in незрелый.**
  На depth-4 (1M узлов) для зрелой поддержки нужны тысячи итераций (минуты); на ~150
  итерациях прунинг режет живые атакующие опции → стратегия вырождается в «всё в банк/щит».
  Доступен опционально (`prune=true`), но по умолчанию **выключен**: микро-дерево решается
  в полном дереве. Перспектива — warm-start (переиспользование инфосетов между ходами) или
  статическая спектральная абстракция аллокаций (не сделано, рискованно).
- **Большой перф-выигрыш (2 порядка) дал ключ инфосета**: вместо полной истории наблюдений
  на узел — `(текущее наблюдение, prev_iid)` (предыдущий инфосет ходящего; ходы строго
  чередуются → depth-2 предок). Инфосеты идентичны (валидировано), память и хэширование
  упали. Плюс убраны O(инфосетов × узлов) сканы (a_move_of_info, финализация n_acts).
  Итог: сборка depth-4 3v3: 140с → 1.2с; depth=3/200 итер: 20с → 0.16с.

## 8. Ключевые числа и ориентиры

- Практический порог expl: < 0.05 (человек практически не может использовать).
  Достигнут на 1v1 cap=6 (0.0496, 1400 итераций).
- Сходимость убывает по O(1/T)-хвосту: гнаться за 0.005-0.001 дорого и не нужно для игры.
- 2v2 — достижимое точное доказательство метода (645k состояний, ноут, минуты).
- 3v3 точная — вопрос ресурсов (сервер + параллелизм), не принципа.
