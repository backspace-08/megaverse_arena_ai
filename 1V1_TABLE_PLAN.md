# План: монолитная универсальная 1v1-таблица (Option A, Rust fixed-point)

Статус: **утверждено направление**, γ и финальный прогон — после смоук-теста (см. §7).
Этот документ — рабочая память сессии. Пока не подтверждено тестом, держать под сомнением.

---

## 1. Утверждённые факты о реальной игре (от пользователя, 2026-08-14)

- **Правила бюджета идентичны текущим**: base(t): t1→1, t2-4→2, t5-6→3, t7+→4; банк ≤ 4; суммарный бюджет ≤ 8. Все действия обязаны тратиться.
- **Длины матча ничем не ограничены** (нет капа). → бесконечная игра, γ<1 обязателен для определённости значений.
- **Реальные диапазоны статов ШИРЕ тестовых**: HP ~3000..10000, ATK ~1000..3500 (мультипликатор 0.7..1.3).
  - Проверено brute-force: `max hits = 15` (`ceil(10000/(1000·0.7))=15`). hits ∈ [1..15], сетка **1..16** (16 = буфер).
- **Ключевая поправка к анализу**: «пол из 0»/объективные ничьи из симметричных состояний — **неверно**.
  - Оборона не сустейнится: банк ≤4 и сбрасывается, база 4 → защитник не может всегда блокировать 8-бурст.
  - Раскрытие R (бюджет−атаки−свитч) делает полную защиту информационно эксплуатируемой.
  - Ничьи в текущих результатах (77% на cap-6, ~95% у depth-1) — **артефакты солвера** (кап-6 резал бурст-фазу; material-fallback на turn≥8), а не структура игры.

## 2. Дизайн (итоговый)

Игра делится на две зоны:
- **Фаза 4 (turn ≥ 7)**: база 4, бюджет ∈ [4,8], правила стационарны. Значения **инвариантны к ходу** (бесконечная игра) → один фикспойнт V_∞.
- **Фазы 1-3 (turn 1..6)**: backward induction 6→1 (turn k решается от значений k+1; листья turn 7 → V_∞).

Алгоритм:
1. **Фикспойнт V_∞**: сетка состояний Фазы 4; для каждого состояния — depth-2 FH-CFR (батч-дерево по чанкам), листья = значения этой же таблицы; итерации до Δ<0.001.
2. **Backward induction** фаз 1-3 от V_∞.
3. Выход: таблица `(hitsA,hitsB,bankA,bankB,shA,shB,turn,mover) → value`, покрывает ВСЕ turn 1..100+, **0 material-fallback**.

Канонический матчап для генерации: atk 2000, HP = hits·2000, mult 1.0 (матчап-независимость из SOLVER_STATE §2).
Значения = каноническая вера (допущение); на рантайме belief подхватывается `solve_micro_belief`.

## 3. Параметры и стоимости

- Сетка Фазы 4: 16·16 (hits) · 5·5 (banks) · 5·5 (shields) · 2 (mover) = **320 000 состояний**
  (belief-корни: 16·16·2·5·5·9 = **115 200**).
- Аллокаций на бюджет (1v1, без свитча): b4→15, b5→20, b6→25, b7→30, b8→35.
- Замер: depth-2 ≈ 241 узлов/корень, 4.2M node-visits/s (1 поток); солв 200 итер ~0.018с/дерево.
- Замер смоука (hits 1..7, 22 050 belief-корней, 5 воркеров): **~170с/итерация**, сходимость к 6-7 итерациям (горизонт 12-14 плёсов).
- Полный грид (115 200 корней) ≈ **9-12 мин/итерация** × ~25-30 итераций (адаптивно до δ<0.02) ≈ **4-6 ч** + рампа ≈ 1-1.5 ч.
- **Итого ~5-7 ч одноразово** (ночной прогон). Память: чанки по ~10k корней, 5 воркеров (паттерн gen_value_data).

## 4. γ (дисконт) — РЕШЕНО: γ=1.0 + усечённый горизонт

**ПРЕДЫДУЩЕЕ ОТМЕНЕНО (результат смоук-теста 2026-08-14):** γ<1 фикспойнт при γ≈1 не сходится практически (оператор depth-2 сжимает как γ²≈0.996 → δ застряла ~0.01 после 15 итераций; для δ<0.001 нужно ~1700). Отброшен.

**Модель:** γ=1.0, усечённый горизонт D. Итерация `T_2` (depth-2 солв с листьями = предыдущая таблица) = backward induction; горизонт растёт на 2 плёса за итерацию, сходится детерминированно за D/2 проходов.
- Победа всегда = 1 (максимально «бьётся»), затягивание → ничья 0.
- Вечная черепаха ломается: в конечном горизонте бурст успевает убить.
- Дилемма γ закрыта, настраивать нечего.

**Смоук-подтверждение (hits 1..7, 5 воркеров, ~25 мин):**
- Свежий (5,5) A = +1.0 (гарант. победа), (7,7) A = +0.583 (инициатива первого), стабильно к горизонту 12-14 плёсов.
- A банк/B свежий = +1.0, A свежий/B банк = −0.15 — согласовано с банк-бурст механикой.
- **Вердикт: черепаха проигрывает, игра решаемая, бой реальный.**

## 5. Открытые вопросы

- γ финальный — по смоук-тесту.
- Цена ничьей (сейчас 0): если захотим жёстко давить черепаху — отрицательная цена ничьей (решение по семантике, не солвера). Пока не трогаем.
- Формат выхода: бинарный flat-массив (u32-ключ → f64), CSV оставить для отладки.

## 6. Стек изменений (реализовано)

- **Rust** (`cote_cfr/src/lib.rs`): `V1Layout` (плотный грид belief-состояний),
  `solve_1v1_step(leaf, start, end, root_turn, ...)` — один backward-induction шаг
  по слайсу грида (depth-2 belief-root микро-дерево на состояние, single-thread),
  belief-ключ таблицы `(hA,hB,to_move,own_bank,own_sh,R,turn)` + turn-clamp>=7→7.
  Старый движок (`MicroTree`, `solve_micro`) не тронут.
- **Драйвер** `server/build_1v1_table.py`: фаза 4 (backward induction, γ=1.0) +
  рампа 6→1 + дамп CSV. Параллелизм — **multiprocessing** (потоки НЕ масштабируются:
  общий аллокатор; проверено: 16 потоков ≈ 1 поток, процессы линейны). Чекпоинты
  бинарные после каждой итерации/хода, resume автоматический.
- **Формат таблицы**: belief-ключ (не конкретный 8-мерный) → 7·207 360 =
  1 451 520 строк, ~60МБ. `load_1v1_table` переведён на новый формат.
- `cfr_bot.py` / `gen_value_data.py` указывают на `cote_cfr/1v1_table.csv`
  (gitignored, артефакт сборки). Старый `solver_tree_fh.py` остаётся лабораторией.

## 7. Статус — ТАБЛИЦА СОБРАНА И ВАЛИДИРОВАНА (2026-08-15)

**Прогон на сервере** (64 vCPU Ice Lake, 56 воркеров, `--solve-iters 100`, 35 итераций): фаза 4 ~66с/итерация ≈ 39 мин, рампа ~4.5 мин, CSV <1с. Итого ~45 мин.

**Результат:** `cote_cfr/1v1_table.csv` (35.4 МБ, 1 451 520 строк = 7 ходов × 207 360 belief-состояний).

**Валидация (локально, движком):**
- Загрузка: 1 451 520 строк, все turn 1..7.
- Боевые значения: свежий (5,5)/(7,7)/(16,16) у первого ходящего ≈ +0.999; промежуточные блеф-значения есть (0.2-0.9 ≈ 25% состояний, напр. (1,1,R=8)=-0.66); ~0.15% состояний ≈ 0 (истинные ничьи).
- Рампа: бюджет-эффект виден (turn 1 = +0.57 при бюджете 1, turn 3/5/7 ≈ +1.0).
- Clamp turn≥7: turn 9/12/25 возвращают те же значения, что turn 7.
- Мовер-симметрия: (5,5,A)=+0.9998, (5,5,B)=-0.9998.

**Ключевой вывод:** вечная черепаха проигрывает на всём диапазоне hits 1..16 — первый ходящий доминирует через банк-бурст. Ничьи остались только в ~0.15% состояний.

**Ограничение:** δ фикспойнта осциллировала ~0.12-0.14 на краевых (самых длинных) дуэлях; представительные значения сошлись к iter 15. При желании — продление через resume (`--max-iters 50`, +15 мин).
